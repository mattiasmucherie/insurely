{
  "name": "insurely-investment-collection",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd shared && npm install && cd ../server && npm install && cd ../client && npm install"
  }
}
